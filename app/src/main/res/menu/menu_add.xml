<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/select_app"
        android:title="@string/select_app"
        android:icon="@drawable/ic_select"
        app:showAsAction="always"
        tools:ignore="AlwaysShowAction" />
    <item
        android:id="@+id/save_config"
        android:title="@string/save_config"
        android:icon="@drawable/ic_save"
        app:showAsAction="always"
        tools:ignore="AlwaysShowAction" />
    <item
        android:id="@+id/add_method_config"
        android:title="@string/add_method_string"
        android:icon="@drawable/ic_add"
        app:showAsAction="always"/>
</menu>