1. LSPosed用户请开启储存、xml存储配置(二者开一即可)，否则后台无法读取到配置(在未启动本应用的情况无法读取到数据库内配置，上述情况LSP会出现，EDXposed未出现)
2. 目标应用TargetSdkVersion为31及以上的（获取/写入本应用数据需要目标应用适配），请开启xml存储配置（储存文件也许目标app读取不到）
3. 由上述的1、2将会导致很多问题，如：卸载本应用后，本地文件仍然存在，即使你重新安装后未添加任何配置，仍然存在被hook的可能性；也可能会导致你的配置刷新了，但它有没有刷新。
4. 上述3的问题，建议添加、更改、删除配置一定要在激活本模块的情况下进行（如果你使用了xml存储），原因是：使用new XSharedPreferences需要激活本模块才能正常的使用
5. 遇到配置突然不能生效，请清除数据、卸载应用再导入配置（如果你能看懂1、2、3、4所说的，也大可不必这样做）
6. 由2又导致一个问题：悬浮窗不再能打印dialog、toast调用等，解决方案：自行去xposed框架中查看log
7. 上述所有的问题待解决...