1. LSPosed用户请开启储存文件存储配置，否则后台无法读取到配置(在未启动本应用的情况无法读取到数据库内配置，上述情况测试时LSP会出现，EDXposed未出现)
2. 目标应用api为30及以上的（获取/写入本应用数据需要目标应用适配 https://medium.com/androiddevelopers/package-visibility-in-android-11-cc857f221cd9），请开启Root写入配置
3. 由上述的1、2将会导致很多问题，如：卸载本应用后，本地文件仍然存在，即使你重新安装后未添加任何配置，仍然存在被hook的可能性，所以卸载本应用前，请在设置页面手动删除；
4. 由2又导致一个问题：悬浮窗不再能打印dialog、toast调用等，解决方案：开启Root写入配置
5. 有时不生效？
   储存文件更新配置某些情况下需要手动刷新，开启、关闭、编辑保存即可刷新
6. 什么是smali转配置？
   开启此实验功能后，配置页面顶部会增加‘粘贴板’图标，点击可将应用调用代码或签名，转化为配置（防止手动输入错误），增加配置后你需要手动选择合适的模式以及结果值
   调用代码例子：
   iget v0, p0, Lme/duck/hooktest/bean/UseBean;->level:I
   invoke-virtual {v0}, Lme/duck/hooktest/bean/UseBean;->isHook()Z
   方法签名、字段签名例子：
   Lme/duck/hooktest/bean/UseBean;->level:I
   Lme/duck/hooktest/bean/UseBean;->isHook()Z
   上述可在MT管理器导航中长按字段或方法选择复制签名或者查找调用
7. 部分特性请看更新记录