<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk tools:overrideLibrary="com.github.kyuubiran.ezxhelper" />

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SimpleHookApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SimpleHook"
        tools:targetApi="n">
        <activity
            android:name=".ui.activity.A33PermissionActivity"
            android:exported="false"
            android:label="@string/label_batch_grant" />
        <activity
            android:name=".ui.activity.RecordDetailActivity"
            android:exported="false"
            android:label="@string/app_record" />
        <activity
            android:name=".ui.activity.RecordActivity"
            android:exported="false"
            android:label="@string/app_record" />
        <activity
            android:name=".ui.activity.AboutActivity"
            android:label="@string/app_about"
            android:theme="@style/AppTheme.About" />
        <activity
            android:name=".ui.activity.ExtensionActivity"
            android:label="@string/app_extension"
            android:theme="@style/Theme.SimpleHook" />
        <activity
            android:name=".ui.activity.AppListActivity"
            android:label="@string/app_select_app"
            android:theme="@style/Theme.SimpleHook" />
        <activity
            android:name=".ui.activity.ConfigActivity"
            android:label="@string/app_config"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SimpleHook"
            tools:ignore="LockedOrientationActivity" />
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppSplashScreen"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.ImportConfigActivity"
            android:exported="true">
            <intent-filter tools:ignore="AppLinkUrlError">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <provider
            android:name=".provider.MyProvider"
            android:authorities="${PROVIDER}"
            android:exported="true"
            tools:ignore="ExportedContentProvider" />

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="自定义hook/DIY hook" />
        <meta-data
            android:name="xposedminversion"
            android:value="53" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />
        <meta-data
            android:name="${FLAVOR}"
            android:value="true" />
    </application>

</manifest>